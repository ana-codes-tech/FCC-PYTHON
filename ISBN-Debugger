def validate_isbn(isbn, length):
    # FIX: len() only takes one argument.
    if len(isbn) != length:
        print(f'ISBN-{length} code should be {length} digits long.')
        return


    # FIX: The last character is the check digit.
    # The rest are the main digits.
    main_digits = isbn[:length - 1]
    given_check_digit = isbn[length - 1]


    try:
        # FIX: Catch invalid characters such as '-'
        # Only the check digit may be X (for ISBN-10).
        main_digits_list = [int(digit) for digit in main_digits]
    except ValueError:
        print('Invalid character was found.')
        return


    # FIX: ISBN-10 may end with X.
    if length == 10:
        if not (given_check_digit.isdigit() or given_check_digit == 'X'):
            print('Invalid character was found.')
            return
        expected_check_digit = calculate_check_digit_10(main_digits_list)


    # FIX: ISBN-13 check digit must be numeric.
    else:
        if not given_check_digit.isdigit():
            print('Invalid character was found.')
            return
        expected_check_digit = calculate_check_digit_13(main_digits_list)


    # Compare expected and given check digits.
    if given_check_digit == expected_check_digit:
        print('Valid ISBN Code.')
    else:
        print('Invalid ISBN Code.')




def calculate_check_digit_10(main_digits_list):
    # No changes needed in this function.
    digits_sum = 0


    for index, digit in enumerate(main_digits_list):
        digits_sum += digit * (10 - index)


    result = 11 - digits_sum % 11


    if result == 11:
        expected_check_digit = '0'
    elif result == 10:
        expected_check_digit = 'X'
    else:
        expected_check_digit = str(result)


    return expected_check_digit




def calculate_check_digit_13(main_digits_list):
    # No changes needed in this function.
    digits_sum = 0


    for index, digit in enumerate(main_digits_list):
        if index % 2 == 0:
            digits_sum += digit
        else:
            digits_sum += digit * 3


    result = 10 - digits_sum % 10


    if result == 10:
        expected_check_digit = '0'
    else:
        expected_check_digit = str(result)


    return expected_check_digit




def main():
    user_input = input('Enter ISBN and length: ')


    try:
        # FIX: Handles missing comma (IndexError).
        values = user_input.split(',')
        isbn = values[0]
        length = int(values[1])


    except IndexError:
        print('Enter comma-separated values.')
        return


    except ValueError:
        # FIX: Handles non-numeric length.
        print('Length must be a number.')
        return


    # FIX: Only 10 and 13 are allowed.
    if length == 10 or length == 13:
        validate_isbn(isbn, length)
    else:
        print('Length should be 10 or 13.')


